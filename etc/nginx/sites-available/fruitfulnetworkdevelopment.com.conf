server {
    server_name fruitfulnetworkdevelopment.com www.fruitfulnetworkdevelopment.com;

    root /srv/webapps/clients/fruitfulnetworkdevelopment.com/frontend;
    index index.html;

    # Serve the main HTML with no-cache so new deployments are visible
    # without needing a cache-busting query string like ?v=2
    location = / {
        add_header Cache-Control "no-cache, must-revalidate";
        try_files $uri $uri/ =404;
    }

    # Default handler for the rest of the paths
    location / {
        try_files $uri $uri/ =404;
    }

    # Optional: later, when your backend is running, you can enable this
    # location /api/ {
    #     proxy_pass http://127.0.0.1:8000;
    #     include proxy_params;
    # }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/fruitfulnetworkdevelopment.com/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/fruitfulnetworkdevelopment.com/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot


}
server {
    listen 80;
    server_name fruitfulnetworkdevelopment.com www.fruitfulnetworkdevelopment.com;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    return 301 https://$host$request_uri;
}
